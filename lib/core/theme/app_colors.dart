import 'package:flutter/material.dart';

/// Light theme colors
class AppColorsLight {
  static const background = Color(0xFFFFFFFF);
  static const foreground = Color(0xFF242424);

  static const card = Color(0xFFFFFFFF);
  static const cardForeground = Color(0xFF242424);

  static const primary = Color(0xFF030213);
  static const primaryForeground = Color(0xFFFFFFFF);

  static const secondary = Color(0xFFF2F2F5);
  static const secondaryForeground = Color(0xFF030213);

  static const muted = Color(0xFFECECF0);
  static const mutedForeground = Color(0xFF717182);

  static const accent = Color(0xFFE9EBEF);
  static const accentForeground = Color(0xFF030213);

  static const destructive = Color(0xFFD4183D);
  static const destructiveForeground = Color(0xFFFFFFFF);

  // Weather app custom
  static const gradientStart = Color(0xFF87CEEB);
  static const gradientEnd = Color(0xFFB0E0E6);

  static const glassBg = Color.fromRGBO(255, 255, 255, 0.55);
  static const glassBorder = Color.fromRGBO(255, 255, 255, 0.5);

  static const textPrimary = Color.fromRGBO(0, 0, 0, 0.9);
  static const textSecondary = Color.fromRGBO(0, 0, 0, 0.75);
  static const textMuted = Color.fromRGBO(0, 0, 0, 0.65);

  static const alertRed = Color(0xFFFF6B6B);
  static const successGreen = Color(0xFF51CF66);
  static const warningOrange = Color(0xFFFFB74D);
  static const accentBlue = Color(0xFF4FC3F7);
  static const accentPurple = Color(0xFFBA68C8);

  static const sosBg = Color(0xFFFF4757);
  static const sosHover = Color(0xFFFF3838);
}

/// Dark theme colors
class AppColorsDark {
  static const background = Color(0xFF242424);
  static const foreground = Color(0xFFFFFFFF);

  static const card = Color(0xFF242424);
  static const cardForeground = Color(0xFFFFFFFF);

  static const primary = Color(0xFFFFFFFF);
  static const primaryForeground = Color(0xFF242424);

  static const secondary = Color(0xFF444444);
  static const secondaryForeground = Color(0xFFFFFFFF);

  static const muted = Color(0xFF444444);
  static const mutedForeground = Color(0xFFB4B4B4);

  static const accent = Color(0xFF444444);
  static const accentForeground = Color(0xFFFFFFFF);

  static const destructive = Color(0xFF991B1B);
  static const destructiveForeground = Color(0xFFFFFFFF);

  // Weather app custom
  static const gradientStart = Color(0xFF1A1A2E);
  static const gradientEnd = Color(0xFF16213E);

  static const glassBg = Color.fromRGBO(255, 255, 255, 0.1);
  static const glassBorder = Color.fromRGBO(255, 255, 255, 0.2);

  static const textPrimary = Colors.white;
  static const textSecondary = Color.fromRGBO(255, 255, 255, 0.9);
  static const textMuted = Color.fromRGBO(255, 255, 255, 0.7);

  static const alertRed = Color(0xFFEF4444);
  static const successGreen = Color(0xFF10B981);
  static const warningOrange = Color(0xFFF59E0B);
  static const accentBlue = Color(0xFF3B82F6);
  static const accentPurple = Color(0xFFA855F7);

  static const sosBg = Color(0xFF991B1B);
  static const sosHover = Color(0xFF7F1D1D);
}
